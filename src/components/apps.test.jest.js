describe(`Apps component`, () => {
    let requiredProps;

    beforeEach(() => {
        requiredProps = [{
            title: 'app title',
            description: 'app-description',
            version: 'app-verion',
            platforms: [],
            date: 'app-date',
            type: 'app-type'
        }];
    });

    it(`should match initial snapshot`, () => {
      // todo
    });
});